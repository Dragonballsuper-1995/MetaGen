# ============================================================
# Core dependencies — pinned for reproducibility
# ============================================================

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Task queue ---
celery==5.4.0
redis==5.2.1

# --- Frontend ---
streamlit==1.41.1
requests==2.32.3

# --- AI / ML ---
# Windows (Py 3.12): prebuilt CUDA wheel for GPU offload
llama-cpp-python @ https://github.com/abetlen/llama-cpp-python/releases/download/v0.3.4-cu124/llama_cpp_python-0.3.4-cp312-cp312-win_amd64.whl ; sys_platform == "win32" and python_version == "3.12"
# Non-Windows fallback: standard wheel
llama-cpp-python==0.3.4 ; sys_platform != "win32"
# Windows CUDA runtime DLLs required by the cu124 wheel
nvidia-cuda-runtime-cu12==12.4.127 ; sys_platform == "win32"
nvidia-cublas-cu12==12.4.5.8 ; sys_platform == "win32"

# --- Configuration & validation ---
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1

# --- Security & rate limiting ---
slowapi==0.1.9

# --- Data collection (dataset_builder.py only) ---
google-api-python-client==2.159.0

# --- Utilities ---
huggingface_hub==0.27.1
